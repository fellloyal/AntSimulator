# 地图尺寸变化后自动居中 - 实施结果 - 2026-06-05

## 改动

| 文件 | 说明 |
|------|------|
| `src/pages/MapEditor.tsx` | 新增 useEffect 监听 `editorMapWidth/editorMapHeight`，地图中心自动对齐画布中心 |

## 关键代码

```ts
useEffect(() => {
  const canvas = canvasRef.current;
  if (!canvas) return;
  const cw = canvas.clientWidth;
  const ch = canvas.clientHeight;
  if (cw === 0 || ch === 0) return;  // 画布尚未布局
  const vp = viewportRef.current;
  vp.offsetX = cw / 2 - (editorMapWidth / 2) * vp.zoom;
  vp.offsetY = ch / 2 - (editorMapHeight / 2) * vp.zoom;
  render();
}, [editorMapWidth, editorMapHeight, render]);
```

## 验证

| 检查 | 结果 |
|------|------|
| `npm run check` | ✓ 通过 |

## 行为

- 打开编辑器：地图居中显示
- 切换预设：自动重新居中
- 改自定义尺寸：自动重新居中
- 用户手动拖动/缩放/窗口 resize：保留用户当前位置，不重置

## 提交

`c6cbe72` fix(editor): 地图尺寸变化后自动居中 - 避免用户找不到地图
