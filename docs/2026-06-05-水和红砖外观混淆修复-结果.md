# 水和红砖外观混淆修复 - 实施结果 - 2026-06-05

## 改动

| 文件 | 改动 |
|------|------|
| `src/render/ObstacleRenderer.ts` | 渲染循环跳过 `terrain === 2` 的格子；类型加 `terrain?: number` |
| `src/pages/MapEditor.tsx` | 编辑器渲染循环同步跳过 `terrain === 2` 的格子 |

## 行为

- 水格子：保留水纹（蓝色 + 波浪），不再被红砖覆盖
- 游戏逻辑：水仍为 wall=1，蚂蚁仍不能通过
- 与项目硬约束 "Water terrain is impassable and treated as a wall" 一致

## 验证

| 检查 | 结果 |
|------|------|
| `npm run check` | ✓ 通过 |

## 提交

`7e9b397` fix(render): 水格子不再被红砖纹理覆盖
