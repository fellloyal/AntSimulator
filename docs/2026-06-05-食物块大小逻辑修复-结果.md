# 食物块大小逻辑修复 - 实施结果 - 2026-06-05

## 改动

| 文件 | 说明 |
|------|------|
| `src/pages/MapEditor.tsx` | 重写食物渲染：取消锚点限制，块大小扫描改为检查新增整行整列 |
| `src/render/FoodPileRenderer.ts` | 同上（同步修复游戏运行时） |

## 修复前后对比

| 场景 | 修复前 | 修复后 |
|------|------|------|
| 单个 2x2 (small) | 小图 ✓ | 小图 ✓ |
| 两个相邻 2x2 (在 (0,0) 和 (0,2)) | 第一个画 4x4 大图，第二个不画 ✗ | 各画 2x2 小图 ✓ |
| 重叠 2x2+3x3 (中心点重叠) | 大图 (取决于对齐) | 各画对应的块 ✓ |
| 1xN 纵向条 | 误判为大图 | N 个 1x1 小图 |
| L 形 | 部分丢失 | 每个连通区域都画 |

## 验证

| 检查 | 结果 |
|------|------|
| `npm run check` | ✓ 通过 |

## 关键代码变化

**新算法核心**：
```ts
let blockSize = 1;
while (x + blockSize < gridW && y + blockSize < gridH) {
  let allFood = true;
  // 检查新行
  for (let bx = 0; bx <= blockSize; bx++) {
    const c = foodCellMap.get((y + blockSize) * gridW + (x + bx));
    if (!c || c[3] !== type) { allFood = false; break; }
  }
  if (!allFood) break;
  // 检查新列
  for (let by = 0; by <= blockSize; by++) {
    const c = foodCellMap.get((y + by) * gridW + (x + blockSize));
    if (!c || c[3] !== type) { allFood = false; break; }
  }
  if (!allFood) break;
  blockSize++;
}
```

从 N=1 扩展到 N+1 时，新增的**整行** y+N 和**整列** x+N 都必须是同类型食物，才能形成真正的 N+1 方块。

## 提交

`5def5aa` fix(render): 食物块大小逻辑修复 - 取消锚点+正确NxN扫描
